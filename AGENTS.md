# Agent Instructions

## Git Branches

- Do not use agent, assistant, vendor, or tool branding in branch names.
- In particular, do not use `codex` in branch names.
- Prefer short feature-oriented branch names, for example:
  - `availability-discovery-0.4.0`
  - `resource-availability`
  - `vm-check-output`
